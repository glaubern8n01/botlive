# Dependencias so dos agentes de API. Nao entram na imagem do vigia.
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
# narracao local (Piper roda em onnxruntime, CPU, sem GPU)
piper-tts==1.7.0
pillow==12.1.1
# Download em massa (Producao em Massa). Sem pin pelo mesmo motivo do
# requirements.txt do vigia: precisa acompanhar as mudancas das plataformas.
# Sem ele o modulo sobe, mas todo download falha - ffmpeg sozinho nao baixa.
yt-dlp
# Campanhas de Cortes: o upload de material autorizado usa multipart/form-data,
# e sem esta biblioteca o FastAPI recusa a rota inteira em tempo de execucao.
python-multipart>=0.0.20,<1
